"""SQLite connection helper.

TODO: implement get_connection() that opens (and initializes from
schema.sql on first run) the local SQLite database. Path should come
from config, not be hardcoded.
"""
